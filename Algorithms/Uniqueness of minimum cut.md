# Prompt
>"You are given a directed flow network $G=(V,E,c,s,t)$ with distinct integral capacities on all edges

### Part a
>"Is the minimum $s-t$ cut guaranteed to be unique? Justify.

### Part b
>"Suppose you compute a max flow $f$. Describe an $O(E)$ procedure to determine whether the min cut is unique

