# Prompt
>"Let $G$ be a flow network with integer capacities, and $f$ a maximum flow

### Part a
>"If an edge $e$ is saturated under $f$, is $e$ necessarily upper-binding? Give a short counterexample or a one-sentence proof

### Part b
>"Describe an $O(E)$ algorithm that determines whether a given edge $e$ is upper-binding